- [x] Modifier `server.js` pour activer `sequelize.sync()` quand `process.env.DB_SYNC === 'true'`.
- [ ] Démarrer l’API en local avec `DB_SYNC=true`.
- [ ] Vérifier dans phpMyAdmin que la DB `wins_agency` affiche les tables.
- [ ] Exécuter `node seed.js` si le superadmin / données ne sont pas présentes (avec `DB_SYNC=true`).

